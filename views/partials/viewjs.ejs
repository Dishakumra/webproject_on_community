	var usere=[];
	 var currentuser=window.location.href.substring(window.location.href.lastIndexOf('/') + 1).replace("-"," ");
function funcNav()
{
	var div2=document.getElementById('div2');
	if(div2.style.width=="250px")
		div2.style.width="50px";
	else
		div2.style.width="250px";
}
function switchtouser()
{
	window.location=`/switchtouser/${currentuser}`;
}
function switchtoadmin()
{
	window.location=`/switchtoadmin/${currentuser}`;
}
function commlist()
{
	window.location=`/communitylist/${currentuser}`;
}
function commpanel()
{
	window.location=`/commpanel/${currentuser}`;
}

function edituser()
{


	window.location='/home-inner';
}
function adduser()
{
	document.getElementById("act1").classList.remove("active");
	document.getElementById("add1").classList.add("active");
	window.location=`/2/${currentuser}`;

}
var modal=document.getElementById("mymodal");
var content=document.getElementById("modal");
function reset()
{
	modal.style.display = "none";
}
function rollback()
{
	window.location='/login.html';
}

function logout()
{
  modal.style.display = "block";
}

window.onclick = function(event) {
  if (event.target == modal) {
    content.classList.add("applyshake");
  }
};
content.addEventListener("animationend", (e) => {
    content.classList.remove("applyshake");
});
function createtag()
{
	console.log("Fd");
	window.location=`/createtag/${currentuser}`;
}
function changepassword()
{
	window.location=`/7/${currentuser}`;
}
function homepage()
{
	window.location=`/1/${currentuser}`;
}
function userlist()
{
	window.location=`/3/${currentuser}`;
}
